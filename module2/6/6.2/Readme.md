Задание №6.2

Задание

Доработать решение задачи 6.1 (список контактов) так, чтобы
структуры и функции по работе с двухсвязным упорядоченным списком
находились в динамической библиотеке.

Выполненная работа

В ходе выполнения задания была выполнена переработка программы телефонной книги.

1. Создана динамическая библиотека

В отдельный модуль были вынесены все структуры и функции, отвечающие за работу с двухсвязным упорядоченным списком контактов.
На их основе была собрана динамическая библиотека `libbook.so`.

2. В библиотеку вынесены

* структура `contact`;
* функция `AddContact()` — добавление нового контакта;
* функция `EditContact()` — редактирование существующего контакта;
* функция `DeleteContact()` — удаление контакта;
* функция `WatchContact()` — получение списка контактов.

Все операции над двухсвязным списком выполняются внутри библиотеки.

3. Основная программа

Файл `main.c` содержит пользовательское меню и обработку ввода данных.
При выборе пунктов меню вызываются функции из динамической библиотеки для выполнения необходимых операций над списком контактов.

4. Организация списка

Контакты хранятся в двухсвязном списке.

При добавлении нового элемента поддерживается упорядоченность списка:

* сначала по фамилии;
* при совпадении фамилии — по имени.

5. Тестирование

Для проверки корректности работы библиотеки создан файл `test.c`, в котором проверяются:

* добавление контактов;
* сохранение сортировки;
* редактирование данных;
* удаление контактов;
* попытка удаления несуществующего контакта.

Для автоматической проверки используются функции `assert()`.

6. Сборка проекта

Сборка проекта выполняется с помощью `Makefile`, который:

* компилирует исходные файлы;
* создает динамическую библиотеку `libbook.so`;
* собирает основную программу;
* собирает тестовую программу.

Результат

В результате выполнения задания структура данных и функции работы с двухсвязным упорядоченным списком были успешно перенесены в динамическую библиотеку.
Основная программа использует библиотеку для выполнения всех операций над телефонной книгой, что делает проект более модульным и удобным для сопровождения.
